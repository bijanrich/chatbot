<div class="bg-gray-50 min-h-screen flex flex-col items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8 bg-white p-8 rounded-xl shadow-md">
    <div>
      <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-green-100">
        <svg class="h-8 w-8 text-green-600" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
      </div>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        Subscription Successful!
      </h2>
      <p class="mt-2 text-center text-sm text-gray-600">
        Thank you for subscribing to FanPilot
      </p>
    </div>

    <div class="bg-gray-50 p-4 rounded-md">
      <h3 class="text-lg font-medium text-gray-900">Subscription Details</h3>
      <div class="mt-2 text-sm text-gray-600">
        <div class="flex justify-between py-1">
          <div>Plan:</div>
          <div class="font-medium"><%= @subscription.plan_name.titleize %></div>
        </div>
        <div class="flex justify-between py-1">
          <div>Status:</div>
          <div class="font-medium text-green-600"><%= @subscription.status.capitalize %></div>
        </div>
        <div class="flex justify-between py-1">
          <div>Billing Period:</div>
          <div class="font-medium"><%= @subscription.interval == 'month' ? 'Monthly' : 'Yearly' %></div>
        </div>
        <% if @subscription.current_period_end.present? %>
          <div class="flex justify-between py-1">
            <div>Next Billing Date:</div>
            <div class="font-medium"><%= @subscription.current_period_end.strftime("%B %d, %Y") %></div>
          </div>
        <% end %>
      </div>
    </div>

    <div>
      <p class="text-sm text-gray-600 mb-4">
        A confirmation email has been sent to your billing email address. You now have full access to all features included in your subscription plan.
      </p>
    </div>

    <div class="flex items-center justify-center">
      <%= link_to "Go to Dashboard", dashboard_index_path, class: "inline-flex items-center px-4 py-2 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
    </div>

    <div class="pt-4 text-center text-xs text-gray-500">
      <p>If you have any questions about your subscription, please contact support at support@fanpilot.app</p>
    </div>
  </div>
</div>
