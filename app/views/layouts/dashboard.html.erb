<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>FanPilot Dashboard</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "dashboard_modern", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    
    <!-- Load Inter font from Google Fonts -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">
  </head>

  <body class="bg-gray-100 dashboard-layout" data-turbo-suppress-warning>
    <%= render 'shared/sidebar' %>
    
    <div class="dashboard-content">
      <%= render 'shared/flash_messages' if flash.any? %>
      
      <main class="dashboard-main">
        <%= yield %>
      </main>
    </div>
  </body>
</html> 